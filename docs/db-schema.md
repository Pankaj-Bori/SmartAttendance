# Database Schema

## Tables:
- Students (id, name, rfid, class, etc.)
- Attendance (id, rfid, timestamp)
- BusRoutes (id, routeName, stops)
- BusLocation (id, busId, latitude, longitude, timestamp)
- Notifications (id, userId, message, status)

